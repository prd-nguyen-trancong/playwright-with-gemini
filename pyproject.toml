[tool.poetry]
name = "olivia-auto-playwright"
version = "0.1.0"
description = "Automation scripts for Paradox Okta access requests using Playwright"
authors = ["Paradox Dev"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
playwright = "^1.58.0"
python-dotenv = "^1.2.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
