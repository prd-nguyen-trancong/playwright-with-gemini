# Gemini AI Automation
# No cookies needed! Gemini uses a persistent browser profile.
# On first run, a browser window opens for Google login.
# After that, the session is saved in .browser-data/gemini/
# To re-login: ai-ask --login "hello"
